import json import os import sys from dotenv import load_dotenv from utils.gemini_utils import GeminiClient from utils.discord_utils import DiscordClient load_dotenv() def resolve_env_vars(config): """Recursively replaces 'ENV_VAR' or strings starting with 'DISCORD_WEBHOOK' with environment variables.""" if isinstance(config, dict): return {k: resolve_env_vars(v) for k, v in config.items()} elif isinstance(config, list): return [resolve_env_vars(i) for i in config] elif isinstance(config, str): if config.isupper() or config.startswith("DISCORD") or config.startswith("FB") or config.startswith("IG"): return os.environ.get(config, config) return config def main(): config_path = "config.json" if not os.path.exists(config_path): print(f"Error: {config_path} not found.") sys.exit(1) with open(config_path, "r") as f: raw_config = json.load(f) config = resolve_env_vars(raw_config) gemini_key = os.environ.get("GEMINI_API_KEY") if not gemini_key: print("Error: GEMINI_API_KEY environment variable not set.") sys.exit(1) gemini = GeminiClient(gemini_key) discord = DiscordClient() for company in config["companies"]: client_id = company["client_id"] name = company["name"] focus_areas = company.get("focus_areas", []) keywords = company.get("keywords", []) webhook_url = company.get("discord_webhook_url") print(f"Generating content for {name} ({client_id})...") try: caption = gemini.generate_caption(name, focus_areas, keywords) image_path = gemini.generate_social_image(name, focus_areas, keywords) if not webhook_url or "DISCORD_WEBHOOK" in str(webhook_url): print(f"Skipping {name}: discord_webhook_url not resolved.") continue print(f"Sending to Discord for {name}...") discord_content = f"**DRAFT POST FOR {name}**\n\n{caption}\n\nClient ID: `{client_id}`" discord.send_via_webhook(webhook_url, discord_content, image_path) print(f"Successfully generated and sent draft for {name}.") except Exception as e: print(f"Error processing {name}: {e}") if __name__ == "__main__": main()
